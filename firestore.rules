rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /chat/{document=**} {
      allow read, write: if true;  // Allow anyone to read and write to the chat collection
    }
  }
}